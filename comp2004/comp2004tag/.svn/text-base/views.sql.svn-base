CREATE VIEW currentstudents AS SELECT * FROM ds_students WHERE status = 'current';
CREATE VIEW suspendedstudents AS SELECT * FROM ds_students WHERE status = 'suspended';
CREATE VIEW leftstudents AS SELECT * FROM ds_students WHERE status = 'left';
